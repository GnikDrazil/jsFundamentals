/*
    Functions

        -functions are a chunk of code that perform a set or set of actions when called (or invoked)
        -functions are reusable - we can call them as many times aw we want and they cantake in different sets of data
*/

function hi(){
    console.log('hello!');
}

hi();